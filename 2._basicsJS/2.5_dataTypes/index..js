/*
Что выведет этот скрипт?
*/

const name = "Ilya";

alert( `hello ${1}` ); // Вывод - hello 1

alert( `hello ${"name"}` ); // Вывод - hello name, так как в фигурные скобки вставляется строка

alert( `hello ${name}` ); // Вывод - hello Ilya, так как в фигурные скобки вставляется переменная